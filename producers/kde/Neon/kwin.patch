--- a/src/inputmethod.cpp	2026-05-12 10:26:46.000000000 +0000
+++ b/src/inputmethod.cpp	2026-06-24 16:07:19.571120919 +0000
@@ -505,6 +505,14 @@
     }
 }
 
+void InputMethod::commitText(const QString &text)
+{
+    if (text.isEmpty()) {
+        return;
+    }
+    commitString(m_serial++, text);
+}
+
 void InputMethod::commitString(quint32 serial, const QString &text)
 {
     if (auto t2 = waylandServer()->seat()->textInputV2(); t2 && t2->isEnabled()) {
--- a/src/inputmethod.h	2026-05-12 10:26:46.000000000 +0000
+++ b/src/inputmethod.h	2026-06-24 16:07:19.571367676 +0000
@@ -71,6 +71,8 @@
 
     void commitPendingText();
 
+    void commitText(const QString &text);
+
     // for use by the QPA
     InternalInputMethodContext *internalContext() const
     {
--- a/src/backends/CMakeLists.txt	2026-05-12 10:26:46.000000000 +0000
+++ b/src/backends/CMakeLists.txt	2026-06-24 16:07:19.571404526 +0000
@@ -1,3 +1,4 @@
+add_subdirectory(anland)
 add_subdirectory(drm)
 add_subdirectory(fakeinput)
 add_subdirectory(libinput)
--- a/src/main_wayland.cpp	2026-05-12 10:26:46.000000000 +0000
+++ b/src/main_wayland.cpp	2026-06-24 16:07:19.571533592 +0000
@@ -10,6 +10,7 @@
 #include "backends/drm/drm_backend.h"
 #include "backends/fakeinput/fakeinput_backend.h"
 #include "backends/libinput/libinput_backend.h"
+#include "backends/anland/anland_backend.h"
 #include "backends/virtual/virtual_backend.h"
 #include "backends/wayland/wayland_backend.h"
 #include "core/session.h"
@@ -180,6 +181,9 @@
         if (optionValue == QLatin1String("wayland")) {
             m_platform = new WaylandBackend(this);
         }
+        if (optionValue == QLatin1String("anland")) {
+            m_platform = new AnlandBackend(this);
+        }
     }
